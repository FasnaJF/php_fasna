Setup Project

Do a composer install

`composer install`

Create a DB and run the migrations 

`php artisan migrate`

Seed the tables

`php artisan db:seed`

Finally, start the app

`php artisan serve`




